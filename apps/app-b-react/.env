# URLs of the two Symfony apps (as reached from the browser on the host).
# api.js falls back to these same defaults if the vars are not picked up.
VITE_APP_A_URL=http://localhost:8001
VITE_APP_B_URL=http://localhost:8002
VITE_AGENT_URL=http://localhost:8003
